workspace = "boringcache/cli"

[entries.bundler]
tag = "bundler"

[entries.mise]
tag = "mise-installs"
default_path = "/mise/installs"

[entries.node_modules]
tag = "node_modules"

[profiles.bundle-install]
entries = ["bundler"]

[profiles.mise-install]
entries = ["mise"]

[profiles.yarn-install]
entries = ["node_modules"]
