# Proxy And Adapters

This file covers the local cache-registry, the tool adapters, and the serve/runtime internals behind them.

## Core shape

- `cache-registry` is the proxy.
- `cache-registry` is the standalone operator-facing proxy command.
- Adapter commands either start that proxy in the background or hand off to a small helper.
- `src/serve/**` is the real proxy runtime.
- `src/serve/engines/**` is the incremental engine-boundary home for protocol invariants that should not live in route handlers.
- `src/proxy/**` is the CLI-side wrapper around tag derivation, child command launch, and proxy startup.

## Feature map

| Feature | Entry points | Status | Primary code | Key shared modules | Evidence of use | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| Standalone cache proxy | `cache-registry WORKSPACE TAG` with `--port`, `--host`, `--no-platform`, `--no-git`, `--metadata-hint`, `--oci-prefetch-ref`, hidden `--oci-hydration`, `--on-demand`, `--fail-on-cache-error`, `--read-only` | `public-primary` | `src/cli/proxy.rs`, `src/cli/app.rs`, `src/commands/proxy/cache_registry.rs` | `tag_utils`, `proxy::tags`, `serve::runtime`, `serve::state`, `serve::cache_registry` | `tests/integration_tests.rs`, `tests/workspace_injection_tests.rs`, `tests/run_proxy_e2e_tests.rs`, `docs/proxy-mode.md`, `README.md` | Warm-first raw proxy command; `--on-demand` is the expert cold-start override. OCI selected refs index metadata and locator URLs by default, then BuildKit fetches bodies on demand; hidden `--oci-hydration` is for diagnostics and benchmark controls, not normal setup. |
| One-shot proxy wrapper | `run --proxy TAG -- COMMAND...` | `public-primary` | `src/commands/cache/run.rs` | `cache_registry::start_proxy_background`, `proxy::spawn_command`, archive `restore`/`save` helpers | `tests/run_proxy_e2e_tests.rs`, `docs/tool-guides.md`, `docs/contracts/readiness.md` | Temporarily starts the same proxy for one command, waits on shared readiness, then shuts it down |
| Proxy runtime and readiness | same proxy command plus runtime status endpoint behavior | `support-primary`, `internal-only` | `src/serve/runtime/mod.rs`, `src/serve/runtime/listener.rs`, `src/serve/runtime/maintenance.rs`, `src/serve/runtime/shutdown.rs`, `src/serve/http/routes.rs`, `src/serve/http/handlers/mod.rs` | `AppState`, `BlobReadCache`, `PrefetchMetrics`, `OciEngineDiagnostics`, maintenance loop, shutdown handoff | `tests/serve_tests.rs`, `ci/e2e/request-metrics-summary.py`, `docs/proxy-mode.md`, `docs/contracts/readiness.md`, `docs/performance-learning-log.md` | Backs shared readiness state, the hidden ready-file orchestration handoff, `/_boringcache/status`, on-demand bypass, and shutdown publish behavior. Status snapshots expose OCI body-plane metrics plus engine diagnostics for proof sources, graph expansion, range behavior, read-through bytes, publish phase timings, miss causes, and hydration policy. The E2E summary script promotes the same OCI engine keys from `proxy-status-*.json` and real-run `status-*.json` artifacts for post-release analysis, including stable labeled per-snapshot keys such as `request_metrics_status_phase2_restart_warm_oci_body_remote_fetches`. |
| Registry protocol dispatch | proxy-side OCI/Bazel/Gradle/Maven/Nx/Turborepo/sccache/Go protocol handling | `support-primary`, `internal-only` | `src/serve/cache_registry/mod.rs`, `src/serve/cache_registry/route.rs`, `src/serve/cache_registry/bazel.rs`, `gradle.rs`, `maven.rs`, `nx.rs`, `turborepo.rs`, `sccache.rs`, `go_cache.rs`; OCI invariants in `src/serve/engines/oci/**`; Bazel AC/CAS invariants in `src/serve/engines/bazel.rs`; Gradle HTTP-cache write status policy in `src/serve/engines/gradle.rs` | `cache_ops`, `kv_publish`, `upload_sessions`, `blob_read_cache`, `AppState`, `serve::engines::{oci,bazel,gradle}` | broad protocol coverage in `tests/serve_tests.rs`, Docker BuildKit E2E coverage in `ci/e2e/required/e2e-docker-buildkit-registry-test.sh`, context in `docs/adapter-cache-profiles.md`, source-rooted plan in `docs/adr/0002-proxy-engine-plan-b.md`, source-to-test ledger in `docs/adapter-spec-guardrails.md` | This is the heart of the proxy feature, but not a separate CLI surface. OCI selected-ref startup indexes metadata and locator URLs by default; BuildKit fetches bodies on demand. `bodies-background` and `bodies-before-ready` remain hidden diagnostic/benchmark modes. The Docker BuildKit E2E harness emits OCI metrics and gates the default path on selected-ref indexing, bounded client remote body fetches, and OCI engine read-through diagnostics. `oci_body_remote_fetches` is the after-ready client read-through gate; `oci_engine_blob_remote_reads` can include startup or background hydration under non-default modes. Selected-ref/body hydration is delegated to `serve::engines::oci::prefetch` instead of KV. Non-OCI adapters keep the existing KV proxy behavior, with supported route hit/miss/error guardrails tracked in `docs/adapter-spec-guardrails.md`. Bazel AC/CAS store identity and CAS digest integrity now live in `serve::engines::bazel`; `cache_registry::bazel` stays HTTP method glue over shared KV. Gradle's official `413 Payload Too Large` oversized-write behavior now lives in `serve::engines::gradle`, while generic KV keeps its default backlog rejection status for Maven and other object-cache adapters. OCI manifest descriptor extraction, content-type selection, subject/referrers helpers, child-manifest classification, manifest cache entry shape, manifest cache lookup, backend restore, pointer decode, blob-locator seeding, and child-manifest staging now live in `serve::engines::oci`; the HTTP manifest handler parses bodies and maps engine outcomes into OCI responses. OCI upload session semantics now live in `serve::engines::oci::uploads`: start, PATCH, closing PUT, mount `201`/`202`, empty finalize reuse, stale offset `416`, and streaming digest verification for one-shot upload bodies; the HTTP upload handler maps typed outcomes to responses and adds digest-valued `ETag` headers for immutable OCI responses. OCI blob HEAD/GET, URL refresh, body-cache lookup, remote read-through, digest/size verification, ranged GET (`206`/`416`), and body prefetch now live in `serve::engines::oci::blobs`. OCI manifest publish now proves every descriptor through `upload-session`, `mounted-session`, `manifest-reference-session`, `local-body-cache`, or `remote-storage`; zero-byte remote-reuse upload sessions are not treated as local proof for non-empty descriptors. Local body-cache hits are materialized into upload sessions so backend publish can still upload bytes when needed. Upload job selection consumes the exact `PresentBlob` proof session id instead of reselecting sessions globally by digest; a `remote-storage` proof cannot be used for an upload request without local bytes. Save, tracked blob upload, pointer upload, confirm, alias, referrer timing, and session cleanup now live in `serve::engines::oci::publish`. |
| KV proxy core | internal KV registry behavior for lookup, write, publish, preload, and flush/shutdown settlement | `support-primary`, `internal-only` | `src/serve/cache_registry/kv/mod.rs`, `src/serve/cache_registry/kv/blob_read.rs`, `index.rs`, `lookup.rs`, `prefetch.rs`, `write.rs`, `flush.rs` | published index state, blob read cache, flush/publish coordination, observability | unit and scenario tests in `src/serve/cache_registry/kv/mod.rs`, `tests/serve_tests.rs`, operational notes in `docs/performance-learning-log.md` | The active tree keeps the remaining publish/refresh orchestration in `flush.rs`; KV exposes a protocol-neutral blob-integrity hook so adapter engines can reject mismatched payloads without adding adapter-specific branches here |
| Adapter command framework | `turbo`, `nx`, `bazel`, `gradle`, `maven`, `sccache`, `go`, `docker` with common adapter flags | `public-primary` | `src/cli/adapters.rs`, `src/commands/adapters/command/mod.rs`, `src/commands/adapters/mod.rs` | `project_config`, `command_support`, `proxy`, `serve`, `save`, `restore`, `tag_utils`, `git` | `tests/integration_tests.rs`, `docs/adapter-commands.md`, `docs/github-actions.md`, `docs/tool-guides.md` | Shared dry-run JSON, repo-config resolution, proxy startup, and command execution |
| Docker adapter | `docker` with `--cache-mode`, `--cache-ref-tag`, hidden `--oci-hydration`, shared adapter flags | `public-primary` | `src/commands/adapters/command/docker.rs`, `src/commands/adapters/command/mod.rs` | `ProxyContext`, `cache_registry::start_proxy_background`, `proxy::command`, Docker plan resolver | `src/commands/adapters/command/docker.rs` tests, `tests/run_command_tests.rs`, `tests/run_proxy_e2e_tests.rs`, `docs/tool-guides.md` | `--tag` is the proxy cache tag; `--cache-ref-tag` is the OCI cache tag. Read-only Docker adapter runs use on-demand proxy startup and do not auto-prefetch the cache ref, so restore-only CI jobs avoid full tag hydration before BuildKit asks for blobs. The default selected-ref path indexes metadata and locator URLs, then BuildKit fetches bodies on demand. |
| Turbo/Nx/sccache/Go wrapper env wiring | `turbo`, `nx`, `sccache`, `go` | `public-primary` | `src/commands/adapters/command/turbo.rs`, `nx.rs`, `sccache.rs`, `go.rs` | `ProxyContext`, environment injection, `PROXY_AUTH_TOKEN` | `tests/run_proxy_e2e_tests.rs`, per-tool docs in `docs/tool-guides.md` | These are more opinionated than Bazel/Gradle/Maven because they wire more env/protocol details directly. The sccache adapter sets `SCCACHE_WEBDAV_ENDPOINT` and `SCCACHE_WEBDAV_KEY_PREFIX`; `sccache-key-prefix` in repo config maps to the WebDAV root/key-prefix behavior from sccache/OpenDAL. E2E helpers clear the WebDAV endpoint, key prefix, basic auth, and bearer token env vars before starting local proxy-backed sccache runs. |
| Bazel/Gradle/Maven passthrough wrappers | `bazel`, `gradle`, `maven` | `public-primary` | `src/commands/adapters/command/bazel.rs`, `gradle.rs`, `maven.rs` | shared adapter lifecycle in `command/mod.rs`, proxy startup | protocol tests in `tests/serve_tests.rs`, local runtime E2E through `ci/e2e/e2e-local-adapter-commands.sh`, tool guides in `docs/tool-guides.md` | Real support, but thinner wrappers because more config stays in the tool or repo. Bazel's source-backed AC/CAS guardrails are captured in `docs/bazel-mistake-ledger.md`; `serve::engines::bazel` now owns AC/CAS namespace identity and CAS digest integrity. Gradle/Maven source-backed guardrails are captured in `docs/gradle-maven-mistake-ledger.md`; `serve::engines::gradle` owns Gradle's official nonfatal `413` oversized-write status while Maven keeps generic KV semantics plus required `HEAD` support. Required Bazel E2E asserts cache-op GET records/hits instead of relying only on Bazel log text. Gradle and Maven runtime E2E are selected via `LOCAL_ADAPTER_TOOLS` and run through mise-provided Java/Maven when available. |
| Go cacheprog helper | `go-cacheprog --endpoint ... [--token ...]` with env fallbacks | `lightly-used` | `src/commands/adapters/go_cacheprog.rs`, `src/cli/proxy.rs`, `src/cli/app.rs` | direct HTTP client path, used by `go` adapter and manual proxy setups | unit tests in `src/commands/adapters/go_cacheprog.rs`, `docs/tool-guides.md`, `docs/proxy-mode.md` | Supported advanced helper for manual `GOCACHEPROG` wiring, not a headline adapter workflow |
| Tag resolution and registry-root derivation | shared across proxy and adapters via `--no-platform`, `--no-git`, `--tag`, `--endpoint-host` | `support-primary` | `src/tag_utils.rs`, `src/proxy/tags.rs`, `src/serve/http/oci_tags.rs`, parts of `src/commands/proxy/cache_registry.rs` | `platform`, `git`, registry root tag hashing, OCI tag helpers | unit tests in `src/tag_utils.rs`, `src/commands/proxy/cache_registry.rs`, `src/serve/http/handlers/mod.rs` | OCI manifest refs are scoped under the registry root tag, with legacy unscoped restore fallback, so `cache:buildcache` cannot collide across configured registry proxy tags |

## Current active internals

These are internal, but they are clearly active, not dead:

- `src/serve/runtime/**`
- `src/serve/engines/**`
- `src/serve/http/**`
- `src/serve/cache_registry/**`
- `src/serve/state/**`
- `src/proxy/command.rs`
- `src/proxy/tags.rs`

OCI manifest reads must prove the referenced descriptor set before serving the manifest to clients like BuildKit. If the verified blob download-url response reports any missing descriptors, the proxy treats the manifest ref as a miss and does not populate the manifest cache or blob locator.

## Thin or weakly connected surfaces in this family

- `go-cacheprog` is supported, but only as an advanced helper behind `go` or manual `GOCACHEPROG` setups
- `bazel`, `gradle`, and `maven` are supported, but thinner wrappers than `docker`, `turbo`, `nx`, and `sccache`
- workspace shorthand preprocessing is support glue, not a separate product feature

## Current tree note

The KV path is active and still concentrated in `src/serve/cache_registry/kv/flush.rs` and `src/serve/cache_registry/kv/mod.rs`. Update this file if those responsibilities split into new helper modules later.
