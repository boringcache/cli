# OCI / KV Path Audit

This audit answers whether OCI uses KV as its protocol engine.

Short answer: no. OCI registry requests do not go through `KvNamespace`, `put_kv_object`, or `get_or_head_kv_object`. The reason to audit KV before OCI engine moves is that startup still runs through one shared warmup entrypoint before splitting into OCI selected-ref hydration and KV full-tag hydration. Keep that coordination thin: protocol decisions belong to `serve::engines::oci`, even when both paths share runtime scheduling or the local byte cache.

| Area | Current OCI contact | Classification | Next action |
| --- | --- | --- | --- |
| `src/serve/cache_registry/kv/**` | No OCI route uses `KvNamespace` or KV object PUT/GET. `kv/prefetch.rs` now delegates selected OCI refs to `serve::engines::oci::prefetch` before continuing KV full-tag warmup. OCI body hydration no longer uses a KV-named body helper. | KV adapter engine plus a thin startup coordination call. | Do not add OCI behavior here. Keep selected-ref and body-hydration policy in `serve::engines::oci`; keep KV full-tag hydration isolated for object-cache adapters. |
| `src/serve/cache_registry/kv_publish.rs` | No OCI call path. KV flush uses this to upload object-cache blobs. | KV-specific publish helper. | Leave out of OCI. OCI publish should continue through its own `PresentBlob` proof path. |
| `src/serve/cas_publish.rs` | Used by both KV flush and `serve::engines::oci::publish` after `save_entry`. | Protocol-neutral CAS publish substrate: upload blobs, upload pointer/manifest, confirm. | Keep shared. OCI publish owns proof selection, phase diagnostics, alias/referrer orchestration, and calls this helper for the neutral sequence. |
| `src/serve/state/blob_read_cache.rs` | OCI blob GET, mounted blob reuse, local body-cache proofs, and startup body hydration share this cache with KV object reads. | Protocol-neutral local byte cache. | Keep shared for bytes only. OCI manifest metadata now lives in `src/serve/engines/oci/manifest_cache.rs`, with `AppState` holding the runtime map. |
| `src/serve/state/blob_locator.rs` | OCI manifest resolution seeds `(name, digest) -> cache_entry_id/download_url`; `serve::engines::oci::blobs` refreshes and reads it for HEAD/GET and body hydration. | OCI blob locator, not KV. | Keep as shared state owned by the OCI engine path; do not route locator refreshes through KV. |
| `src/serve/state/upload_sessions.rs` | OCI upload start/PATCH/final PUT, mount reuse, child-manifest staging, and local-body-cache proof materialization use this store. | OCI upload/session substrate. | Keep under OCI engine ownership; do not route through KV. |
| `src/serve/http/handlers/manifest.rs` | Parses manifest requests, reads request bodies, and maps engine outcomes into OCI HTTP responses. Manifest cache lookup, backend restore, pointer decode, blob-locator seeding, referrers descriptor loading, and child-manifest staging now live in `serve::engines::oci::manifests`; publish orchestration lives in `serve::engines::oci::publish`. | HTTP glue. | Keep it thin. Do not put manifest resolution, referrers state, or descriptor graph decisions back into the handler. |
| `src/serve/http/handlers/blobs.rs` | Thin wrapper around `serve::engines::oci::blobs`. | HTTP glue. | Keep it thin. Blob locality, URL refresh, digest/size verification, read-through, ranged GET, and diagnostics belong in the blob engine. |
| `src/serve/runtime/mod.rs` and `src/serve/cache_registry::prefetch_manifest_blobs` | Runtime startup still calls the legacy warmup function name, but that function delegates selected OCI refs to `serve::engines::oci::prefetch` before KV full-tag warmup. | Shared startup scheduling with isolated protocol work. | Rename/split the outer runtime entrypoint later if useful; do not share body-hydration code between OCI and KV for convenience. |
| `src/serve/http/handlers/mod.rs` proxy status | Reports KV pending/flush settlement plus OCI body and OCI engine diagnostics in one status payload. | Shared proxy status surface, not protocol truth. | Keep status unified, but feed it OCI diagnostics from the OCI engine and KV diagnostics from KV state. |

## Boundary Rule

KV remains justified for object-cache adapters such as sccache, Gradle, Maven, Turbo, Nx, Go, and Bazel. OCI should only share protocol-neutral substrate: local blob byte cache, backend blob URL resolution, transfer clients, CAS publish sequencing, metrics plumbing, and runtime task scheduling. OCI manifest graphs, blob range semantics, upload sessions, referrers, digest response metadata, and BuildKit cacheconfig behavior belong in `serve::engines::oci`.
